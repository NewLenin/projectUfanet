{
  "name": "nodebootstraper",
  "version": "5.0.0",
  "dependencies": {
    "body-parser": "*",
    "express": "*",
    "image-to-base64": "github:renanbastos93/image-to-base64",
    "method-override": "*",
    "morgan": "*",
    "pug": "*",
    "serve-favicon": "*",
    "stylus": "*"
  },
  "engines": {
    "node": "*",
    "npm": "*"
  },
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  }
}
